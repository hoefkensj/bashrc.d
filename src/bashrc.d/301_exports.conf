#!/usr/bin/env bash
# ############################################################################
# # REPO: bash_bashrcd                                   AUTHORS: hoefkensj@ #
# # FILE: 301_exports.conf                               UPDATED: 2026-07-14 #
# ############################################################################
#

# Terminal
## Xterm
export term="xterm-256color"

# Default Editor:
export EDITOR="/usr/bin/env nvim"

# NVIDIA
## NVIDIA Vulkan
export VK_ICD_FILENAMES="/usr/share/vulkan/icd.d/nvidia_icd.json"

# SSH
## SSH-AGENT
export GPG_TTY=$(tty)
export SSH_AUTH_SOCK="$(gpgconf --list-dirs agent-ssh-socket)"

# MANPAGER
# Bat as Default Manpager 
export MANPAGER="sh -c 'col -bx | bat -l man -p'"

# PATH
# *"...:"* to check exists if a nested dir might exist eg: if you add your /home/$USER it would also match /home/$USER/.bin without the :
BINDIR="/opt/bin/"
[[ "${PATH}" != *"${BINDIR}:"* ]] && export PATH="${BINDIR}:${PATH}"
BINDIR="/opt/local/bin"
[[ "${PATH}" != *"${BINDIR}:"* ]] && export PATH="${BINDIR}:${PATH}"
BINDIR="/opt/local/shell"
[[ "${PATH}" != *"${BINDIR}:"* ]] && export PATH="${BINDIR}:${PATH}"
BINDIR="${HOME}/.cargo/bin"
[[ "${PATH}" != *"${BINDIR}:"* ]] && export PATH="${BINDIR}:${PATH}"
BINDIR="${HOME}/.bin"
[[ "${PATH}" != *"${BINDIR}:"* ]] && export PATH="${BINDIR}:${PATH}"
BINDIR="${HOME}/.local/bin"
[[ "${PATH}" != *"${BINDIR}:"* ]] && export PATH="${BINDIR}:${PATH}"
unset BINDIR